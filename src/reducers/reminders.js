import {} from '../constants';

const initialState = [];

export default function reminders(state = initialState, action) {
    switch (action.type) {

        default:
            return state;
    }
}